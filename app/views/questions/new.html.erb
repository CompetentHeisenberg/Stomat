<h1>Registration</h1>

<%= form_with(model: @question, url: questions_path) do |form| %>
  <div class="field">
    <%= form.label :register %>
    <%= form.text_field :register_field %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_area :password_reg %>
  </div>

  <div class="actions">
    <%= form.submit "Ask Question" %>
    <%= link_to "Back to Home", root_path %>
  </div>
<% end %>
